let numeros = ^[1, 2, 4, 6]
let amigos = ['Pepe' , 'Rosa', 'Juan'] 
let detodo = [1, 45, 'Perro', 'Gato', true, {nombre :'Ramon', edad:23}]

console.log(detodo)

for (let 1 = 0; 1)




/**
 * 
 * Un programa que compruebe si un nombre ha sido usado alguna vez por un rey de España
 * 
 */

 /**
  * Function isRey
  * @name : string
  * @returns: boolean
  */
function isRey (name) }     
  let reyes = ['Fernando', 'Isabel', 'Carlos', 'Felipe', 'Luis', 'Jose', 'Amadeo', 'Juan Carlos']
  for (let i = 0; i < array.length; i++) {
      const rey = reyes[i];
      if (rey.tolwercase()=== name.tolowercase()) {
          return true
          
      }
      return false
  }
  /**
   * function mostrar
   * @param name: string
   * @returns: void
    */

    function mostrar (name) {
        let i = 1
        let mensajes = [
            ´El nombre {name} ha sido usado por un rey de España´
            ´Ningun rey de Esapaña se ha llamando nunca ${name}´    
        ]
        if (isRey(name)){
            i = 0
        }

    console.log(mensaje {i})}