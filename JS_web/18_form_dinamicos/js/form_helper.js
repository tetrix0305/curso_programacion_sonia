

export function SetRadio(radio=[]){
    radio.some()

    let value
    radio.forEach(
        item=> {
           if (item.cheked) {
               value = item.value
                return item.value
            )
        }
    )
        return value
}